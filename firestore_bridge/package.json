{
  "name": "dartsnut-firestore-bridge",
  "version": "1.0.0",
  "description": "Node.js Firestore bridge; Python talks to this executable via Unix socket.",
  "bin": "dist/index.js",
  "scripts": {
    "build:js": "tsc -p tsconfig.json",
    "build:pkg": "npm run build:js && pkg dist/index.js --targets node18-linux-arm64 --output dist/dartsnut_firestore_bridge",
    "build": "npm run build:pkg",
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "pkg": "^5.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}