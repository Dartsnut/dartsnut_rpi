# 更新日志

本项目的所有重要变更都会记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/spec/v2.0.0.html) 规范。

## [1.0.25] - 2026-01-27

### 更改
- 重构：统一 BLE 服务的错误处理逻辑，使其与 WebSocket 服务的标准保持一致

### 新增
- 功能：为 `nmcli` 返回码补充 WiFi 相关错误码

## [1.0.24] - 2026-01-26

### 修复
- 修复：在 `process_widget_fields` 中重新引入图片处理逻辑，并为旧数据增加长度校验

## [1.0.23] - 2026-01-26

### 修复
- 修复：在 `process_widget_fields` 中注释掉图片处理逻辑，避免异常情况

### 文档
- 文档：新增 `ERROR_MESSAGES.md`，并按新的格式和分组策略更新 `ERROR_CODES.md`

### 更改
- 重构：将错误信息统一为 `'message (XX-YY)'` 格式，并按类别归类
- 重构：格式化代码，并为 JSON 读取操作增加字符串截断逻辑

### 新增
- 调试：为 WebSocket 端点增加响应日志
- 功能：改进组件渲染，增加就绪检测及小尺寸组件处理

## [1.0.22] - 2026-01-22

### 新增
- 功能：在 BLE 服务中增加 `locate_device`（定位设备）指令支持
- 功能：实现屏幕变暗逻辑，并支持按键临时恢复亮度
- 功能：增加变暗状态持久化及 WebSocket API，并提供开关配置

### 更改
- 杂项（依赖）：在 `requirements.txt` 中移除 `pybluez` 依赖

## [1.0.21] - 2026-01-20

### 修复
- 修复（下载）：当 `remove_directory` 成功时清理对应 `game_id` 的 `DOWNLOAD_PROGRESS`

### 新增
- 功能（下载）：当相同 `url+md5` 或 `game_id` 已在进行中时返回 `DOWNLOAD_ALREADY_IN_PROGRESS`
- 功能（WebSocket）：通过 `asyncio.to_thread`、接收队列以及“即发即忘”的同步 `download_app` 提升非阻塞能力
- 功能（文件操作）：在下载完成时从 `apps/{game_id}/conf.json` 中读取版本号并写入 `download_progress`

## [1.0.20] - 2026-01-19

### 新增
- 功能：更新 `get_download_progress` 处理逻辑，支持传入 `game_ids` 列表
- 功能：在 `get_download_progress` 中支持多个 `game_id`

## [1.0.19] - 2026-01-16

### 新增
- 功能：为游戏与组件启动命令增加 `--data-store` 参数

## [1.0.18] - 2026-01-16

### 新增
- 功能：在菜单中显示固件更新标记并支持清除
- 功能：在固件更新成功后创建更新标记文件

## [1.0.17] - 2026-01-16

### 更改
- 杂项（计划任务）：在 `setup.sh` 与 `update.sh` 中将定时任务调整为每天凌晨 3 点
- 重构（git 操作）：常规更新使用 `update.sh`，安装相关改动保留在 `setup.sh`

### 新增
- 功能：新增 `update.sh` 脚本，用于依赖更新与服务重启
- 功能：在 `setup.sh` 中增加自动 git 更新的定时任务
- 功能：新增自动检测 git 更新的脚本

### 修复
- 修复：重构组件渲染逻辑，以 `page_img` 作为单一数据源
- 修复（pico8）：修复游戏启动后加载动画卡在屏幕上的问题

## [1.0.16] - 2026-01-13

### 新增
- 功能：增加自动组件更新功能，支持版本检测和后台下载

## [1.0.15] - 2026-01-12

### 新增
- 功能：新增 WebSocket 端点以获取设备亮度与音量

## [1.0.14] - 2026-01-10

_暂无记录的变更_

## [1.0.13] - 2026-01-10

### 新增
- 功能：新增统一错误处理模块，提供错误码及用户友好错误信息
- 功能：新增远程音量控制的 WebSocket 处理器
- 功能：自动记录游戏开始/结束的游玩时长
- 功能：增加持久化用户数据存储及相应 WebSocket 接口
- 功能：调整 `download_app`，要求提供 `url/md5`，并在提供 `game_id` 时使用带进度的异步下载
- 功能（WebSocket 服务）：新增游戏下载 v2 及进度查询接口
- 功能（文件操作）：新增异步游戏下载进度追踪

### 更改
- 重构：更新 WebSocket 服务以使用统一错误处理，并将操作包装在错误处理器中
- 重构：更新各操作模块以使用带错误码的统一错误处理
- 杂项：移除过时的工厂工具配置与资源文件
- 杂项：更新设备亮度配置
- 杂项：更新设备音量设置

### 文档
- 文档：新增完整的错误码参考文档

### 回滚
- 回滚：「从版本控制中移除 apps/conf.json」这一提交

## [1.0.12] - 2025-12-05

### 新增
- 功能：新增游戏选择图片以提升界面效果
- 功能：为上方区域的组件增加大号加载动画
- 功能：为上方区域增加大号加载动画并提升动画帧率至 10fps
- 功能：用动画序列替换静态加载图片
- 杂项：增加包含 7 帧的加载动画图集

### 更改
- 重构：设置界面改用 `font8` 位图字体
- 重构：将 `font12` 重命名为 `font8` 以匹配 8 像素字号
- 杂项：移除未使用的图片文件（`loading.png`, `game_sel1.png`）
- 杂项：更新 `.gitignore`，忽略 `.DS_Store` 并确保忽略 `device.json`
- 调整：更新组件加载图片的位置与大小

### 修复
- 修复：提升网络稳定性及手柄支持
- 修复：更新游戏选择图片以提升画质
- 修复：改进加载动画渲染，并切换为位图字体

## [1.0.11] - 2025-12-03

### 新增
- 更新：增加无线手柄以实现设备控制

## [1.0.10] - 2025-12-01

### 更改
- 更新：调整 SSH 相关操作，并优化在 `main.py` 中读取 `device.json` 的逻辑
- 更新：修改 BLE 名称并更新识别图片

## [1.0.9] - 2025-11-25

### 新增
- 功能：增加 WiFi 管理、状态指示、组件控制与 UDP 广播
- 更新：优化组件进程的暂停与恢复逻辑

## [1.0.8] - 2025-11-21

### 更改
- 更新：将 Pixeldart 刷新率提升至 144Hz，亮度设置为 80%

## [1.0.7] - 2025-11-21

### 更改
- 更新：将 Pixeldart 刷新率调整为 120Hz

## [1.0.6] - 2025-11-20

_暂无记录的变更_

## [1.0.5] - 2025-11-04

### 更改
- 更新：在 `DartsnutRGBMatrix` 中调整 FPS 设置

## [1.0.4] - 2025-10-27

### 修复
- 修复：解决音量为 0 时的音频问题

## [1.0.3] - 2025-10-23

_暂无记录的变更_

## [1.0.2] - 2025-10-21

### 新增
- 更新：在菜单中新增暂停游戏功能
- 更新：在菜单中增加锁图标，并支持按 B 结束游戏

## [1.0.1] - 2025-10-20

### 更改
- 更新：修改 `README.md`

## [1.0.0] - 2025-10-20

### 新增
- 初始正式发布版本

### 更改
- 更新：修改 `README.md`，并更新 `ble_server` 以确保蓝牙已解锁并开启
- 更新：修改 `README.md`
- 更新：在 `requirement.txt` 中增加 `aiohttp`
- 更新：升级到 TRIXIE
- 更新：在 `main.py` 中将 SIGTERM 替换为 SIGKILL
- 更新：更新字体并调整 RGB 矩阵

## [未发布]

_暂无未发布的变更_

---

## [1.0.0 之前] - 2025-08-25 至 2025-10-19

### 初始开发阶段

该阶段包含在 1.0.0 发布前的所有开发提交，大致包括：

- 项目初始化与基础配置
- 核心功能实现
- 服务配置与安装脚本
- 工厂工具的开发
- 蓝牙与网络相关配置
- RGB 矩阵集成
- 游戏与组件系统的实现
- 多轮迭代中的各种改进与优化

